create table user_account (
    accID integer,
    accUN varchar (24),
    accPWD varchar (48),
    ts TIMESTAMP,
    visits integer,
    transactions integer,
    total_transaction_amount integer
   );
